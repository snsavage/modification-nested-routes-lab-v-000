<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name %>
  <%= f.text_field :artist_name %>
  <br />

  <%# <%= f.collection_select :artist_id, Artist.all, :id, :name %1> %>
  <%= artist_select(@song, @artists) %>

  <br />
  <%= f.submit %>
<% end %>
